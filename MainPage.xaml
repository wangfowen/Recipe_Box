<Page
    x:Class="Recipe_Box.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Recipe_Box"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    
    <Grid>
        <Grid.ColumnDefinitions>
			<ColumnDefinition/>
			<ColumnDefinition Width="400"/>
		</Grid.ColumnDefinitions>
		<StackPanel Background="Red" Grid.Column="0" Grid.Row="0">
            <TextBox x:Name="TitleTextBox" Text="Test" Drop="TitleTextBox_Drop" AllowDrop="True" />
        </StackPanel>
        <GridView x:Name="TagGridView" Grid.Column="1" Grid.Row="0" Background="Blue" DragItemsStarting="TagGridView_DragItemsStarting" CanDragItems="True" ItemsSource="{Binding}">
        </GridView>
		</Grid>
</Page>
